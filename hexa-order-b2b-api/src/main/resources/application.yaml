spring:
  application:
    name: HexaOrderB2B
  datasource:
    url: jdbc:h2:mem:management_db
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
